#include <Core.h>

namespace Vinculum {

    int MotionCalculator (relativePathNode& target, float speedPct, float driveTime = 0)
    {
        while (true)
        {

            vex::task::sleep(25);
        }
    }
}